{
  "mcpServers": {
    "resolve-mcp": {
      "command": "uv",
      "args": [
        "run",
        "--directory",
        "${CLAUDE_PLUGIN_ROOT}",
        "python",
        "-m",
        "resolve_mcp"
      ]
    }
  }
}
